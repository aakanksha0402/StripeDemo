<%= form_with(model: plan, local: true) do |form| %>
  <% if plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plan.errors.count, "error") %> prohibited this plan from being saved:</h2>

      <ul>
      <% plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :plan_name %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :plan_amount %>
  </div>

  <div class="field">
    <%= form.label :interval %>
    <%= form.text_field :interval, id: :plan_interval %>
  </div>

  <div class="field">
    <%= form.label :desired_plan_id %>
    <%= form.text_field :desired_plan_id, id: :plan_desired_plan_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
